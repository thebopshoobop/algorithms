from setuptools import setup

setup(
    name="",
    version="0.0.1",
    author="Will Timpson",
    description=(""),
    license="ISC",
    packages=[],
    setup_requires=['pytest-runner'],
    tests_require=['pytest']
)
